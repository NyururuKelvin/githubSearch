import { LatestDirective } from './latest.directive';

describe('LatestDirective', () => {
  it('should create an instance', () => {
    const directive = new LatestDirective();
    expect(directive).toBeTruthy();
  });
});
