import { LatestReposDirective } from './latest-repos.directive';

describe('LatestReposDirective', () => {
  it('should create an instance', () => {
    const directive = new LatestReposDirective();
    expect(directive).toBeTruthy();
  });
});
